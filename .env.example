# GitHub API token for server-side data fetching.
# 로컬에서는 무토큰일 때 경량 수집 + 로컬 캐시가 동작하지만, 실제 테스트 품질을 위해 설정을 권장합니다.
GITHUB_TOKEN=

# OpenAI API key for structured analysis generation.
# 구조화된 분석 JSON 생성에 필요합니다.
OPENAI_API_KEY=

# Optional model override. Defaults to gpt-5-mini.
# 선택값. 기본값은 gpt-5-mini 입니다.
OPENAI_MODEL=gpt-5-mini

# Public site origin used for canonical and Open Graph metadata.
# canonical / OG 메타데이터 기준 주소입니다.
NEXT_PUBLIC_SITE_URL=https://githubprint.vercel.app

# GitHub OAuth app credentials for signed-in self mode.
# 로그인한 본인 계정을 private-enriched mode로 읽기 위한 GitHub OAuth 설정입니다.
# GitHub OAuth App Homepage URL: https://githubprint.vercel.app
# GitHub OAuth App Authorization callback URL: https://githubprint.vercel.app/api/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_SESSION_SECRET=

# Development-only fixture toggle. Never use fake data in production flows.
# 개발 전용 fixture 토글입니다. 프로덕션 경로에서는 fake data를 쓰지 않습니다.
GITHUBPRINT_USE_FIXTURE=0

# Optional internal capture for rule diagnostics; never used for user rankings.
# 여러 조회를 바탕으로 내부 분석 기준을 고도화할 때 사용할 수 있는 로컬/서버 측 snapshot 저장 토글입니다.
GITHUBPRINT_CAPTURE_INSIGHTS=0

# Optional runtime flags with legacy fallbacks still supported in code:
# GITHUBPRINT_ALLOW_RESULT_REFRESH=
# GITHUBPRINT_DISABLE_THROTTLE=

# Optional search-engine ownership verification (the provider's meta content value).
# Google Search Console / Naver Search Advisor / Bing Webmaster Tools에서 발급한 실제 값입니다.
GOOGLE_SITE_VERIFICATION=
NAVER_SITE_VERIFICATION=
BING_SITE_VERIFICATION=
